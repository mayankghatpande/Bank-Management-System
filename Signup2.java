package bank.management.system;

import javax.swing.*;

public class Signup2 extends JFrame {

    // Constructor for the Signup2 class
    Signup2() {
        // Set the size of the JFrame
        setSize(500, 500);

        // Make the JFrame visible
        setVisible(true);

        // Optional: Set default close operation (useful for standalone testing)
        // This will close the application when the window is closed
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }

    // Main method to run the Signup2 class
    public static void main(String[] args) {
        // Create a new instance of Signup2, which will show the JFrame
        new Signup2();
    }
}
